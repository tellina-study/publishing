# План (ШАГ 0) — course-site-launch

> Детальный план под запуск публичного сайта-курса. Работаем строго по нему.

## Подход
Инфраструктурная задача (не piece). Lifecycle адаптируем: BRIEF → BLUEPRINT → **ROAST плана** →
**USER GATE** → инкрементальная сборка (скелет + 1 лекция proof) → гейт → масштабирование.
Три направления = три issue под зонтичным epic. Развязка фаз (A статик / B инфра / C охват) —
главный приём снижения риска. Генератор — один детерминированный скрипт, не фан-аут агентов.
Критика: independent-роаст плана на over-engineering и непроверенные зависимости ДО реализации.

## Делегирование
- **Роаст плана** (independent-агент) — objective: найти over-engineering, непроверенные зависимости
  (CF Pages/Vultr доступ, poppler, git-бლоат, i18n-без-EN), владельцев файлов, дыры изоляции.
  Not-your-job: не переписывать план, не менять зафиксированный стек. Output: SHIP/REVISE/HOLD + находки.
- (Позже, после гейта) перевод EN — субагенты, лекция-за-лекцией.

## Чеклист сессии (до гейта)
- [x] Изучить CLAUDE.md/README publishing-репо + конвенции (pieces/templates/blueprint)
- [x] Разведать исходный контент lessons (PDF/speech/deck), снять «открытые» вводные разведкой
- [x] Ветка + worktree под задачу (`kb-course-site-launch`)
- [x] brief.md (постановка + ship-bar + DO-NOT + открытые вопросы)
- [x] architecture.md (blueprint: фазы, пайплайн, IA, i18n, риски)
- [ ] Завести 4 GitHub issue: epic + дизайн + генератор + публикатор
- [ ] Роаст плана (independent) → внести правки
- [ ] Commit + push ветки + PR (линкует epic)
- [ ] **ПРЕДСТАВИТЬ владельцу план + роаст + открытые вопросы на USER GATE**

## Чеклист ПОСЛЕ гейта (не начинать до одобрения)
- [ ] Скелет MkDocs Material + `mkdocs.yml` + `requirements.txt`
- [ ] `scripts/sync_lectures.py` + тесты (парс deck/speech, pdftoppm)
- [ ] lec-01 RU — proof-of-format на превью, показать владельцу «пощупать»
- [ ] Гейт по формату лекции → масштабирование на 16 лекций
- [ ] CF Pages деплой + домен + SEO/sitemap/hreflang
- [ ] Фаза B: Umami + Remark42 (когда подтверждён Vultr)
- [ ] Close-the-loop: wiki/онтология/decisions

## Риски / на что смотреть
- Vultr/CF/DNS-доступ — непроверенные внешние зависимости (гейт).
- poppler в среде; git-бლოат PNG; i18n-fallback без EN; draft-статус контента.
- Не переусложнять: один скрипт-генератор, стек не пересматриваем.
