# Revision log: 20260923_sdlc-discipline / ru.md

Правка после прожарки (`roast.md`, REVISE, 16 находок), зеркала (`mirror.md`), холодного читателя
(`reader.md`) и второго раунда фактчека (`fact-check-round2.md`). Сквозные решения оркестратора —
`tasks/20260923_sdlc-ai-pitfalls/revision-plan.md` — приняты как есть и не пересматривались.

**Было:** 2154 слова, 6 инлайн-ссылок, 0 списков, 1 выноска, 0 жирных выделений.
**Стало:** 2193 слова, 16 инлайн-ссылок, 5 списков (16 буллетов + 5 нумерованных пунктов),
3 выноски (эпиграф, фрагмент ADR, финальный вопрос), 22 жирных выделения, ни одного абзаца
длиннее 95 слов.

---

## ШАГ 0 — план правок (все пункты отмечены по факту выполнения)

Вход → Требования → Архитектура → Реализация → врезка «Пять вопросов» → Тестирование →
Ревью/доступы → Доставка → финал; сквозное (формат, ссылки, ярлыки, заголовок). Фазовый костяк
не тронут: шесть фаз идут в прежнем порядке, «Пять вопросов» вынесены между фазами как сквозной
инструмент и помечены в тексте как таковые («стоит поперёк всех шести фаз»), поэтому счёт
«шесть фаз — шесть гейтов» не поехал.

---

## По каждому пункту задания

### Факты

**1. METR-2026 — знак и интервалы.** ✅ Закрыт. Абзац 2 переписан: «оценка прироста
продуктивности снова отрицательная — **−18%** у 10 вернувшихся участников и **−4%** у 47 новых,
то есть снова замедление. Но оба доверительных интервала накрывают ноль: от −38% до +9% и
от −15% до +9%». Одна конвенция на все четыре числа, точечные оценки внутри своих интервалов.
Следом — дословная цитата METR про ненадёжный сигнал со ссылкой и причина (30–50% отказываются
брать задачи без ИИ) → «Чистой контрольной группы для этого вопроса больше не составить».
Формулировка «результат оказался никаким» снята.

**2. «40 процентных пунктов».** ✅ Вычеркнуто. Стало: «медианная самооценка ускорения от ИИ
равна 3×, а медианная оценка того, как изменилась ценность их работы, — 1,4–2×. Обе цифры
называет про себя один и тот же человек: ощущаю втрое, готов записать в пользу вдвое».
Арифметики нет. База добавлена: «349 технических специалистов — собственно разработчиков среди
них 87, четверть». Заодно снято «систематическая переоценка» (одна выборка такого слова не несёт).
Авторская глосса «то есть сколько оно реально стоит с поправкой на переделки и проверку» убрана —
источник меряет изменение ценности работы, а не это.

**3. Chroma.** ✅ Ссылка на техотчёт «Context Rot» поставлена, формулировка по round2:
«Качество ползло вниз по мере роста ввода **задолго до предела окна и даже на простых задачах**».
18 моделей с перечислением семейств. Добавлено практическое следствие (его требовал и mirror,
и reader): резать задачу на куски, свежий контекст под каждый, свой критерий готовности.

**4. Thoughtworks.** ✅ Переатрибутировано на «codebase cognitive debt» (Technology Radar, том 34,
апрель 2026, кольцо Caution) с настоящей цитатой про растущий разрыв между реализацией и общим
пониманием команды + ссылка. Blip про рои убран полностью — в тексте не осталось ни слова «рои»,
ни склейки двух записей.

**5. Парафраз Фаулера.** ✅ Вычеркнут целиком. Идею несёт резолвленная цитата Гроува.

**6. Model Spec.** ✅ Ссылка на model-spec.openai.com + отдельная ссылка на публичный CHANGELOG,
цитата «как и наши модели, спека будет постоянно обновляться», семь датированных версий,
предупреждение на старых версиях.

**7. Уиллисон.** ✅ Дословная цитата со ссылкой на пост от 11.03.2025 + вторая половина мысли
(«если вы не видели, как оно запускается, работающей системы нет»). Пересказ снят.

**8. Devin.** ✅ «Cognition в своё время отчиталась, что её агент Devin решает 13,86% задач
SWE-bench. Это 79 задач из 570 — случайной четверти бенчмарка; полный тестовый набор состоит
из 2294 задач. А прежний рекорд, с которым сравнивали, 1,96%, измерен на полном наборе. Два
числа с разными знаменателями, поставленные рядом как одно сравнение». «Удобно выбранной» снято;
атрибуция исправлена (отчиталась сама компания, а не «поймали»).

**9. Replit.** ✅ Осталась одна строка без чисел: «в июле 2025-го агент на инфраструктуре Replit
точно так же снёс продакшн-базу вопреки многократному прямому запрету». «95 из 100» вычеркнуто
совсем. Подробности — у парной статьи.

**10. Amazon Kiro.** ✅ Снят. «Третий подобный инцидент подряд» → «Случай не первый… Два случая —
уже не особенность одного вендора, а класс отказа». Обобщение стоит на Replit + PocketOS.

**11. Абзац про хуки и CVE-2025-59536.** ✅ Вырезан целиком (~120 слов), вместе с цитатой
«Rules in prompts are requests…». Мысль осталась своими словами: «Правило в системном промпте
гейтом не является. Текст, который агент читает, он же волен истолковать иначе; механическим
контроль становится там, где выполнение проверяет не модель, а код вокруг неё» + ровно одна
перекрёстная ссылка на парную статью «Лестница контроля кодинг-агента» (по имени, без URL —
она ещё не опубликована, висячей ссылки не оставляем).

### Содержание и форма

**12. Тезис слишком поздно.** ✅ Закрыт частично-полно, с честной цифрой: тезис был на 361-м
слове, стал на 267-м; поворот («чистой контрольной группы больше не составить») — на 201-м.
Вход разрежен: из METR-2025 остались только «ждали +24%» и «+19% времени» (самооценка «+20%
после» превращена в бесцифровое «закончив, по-прежнему были уверены, что ускорились»), METR-2026
разбит на два коротких абзаца, опрос сжат до трёх предложений. Эпиграфом поднята лучшая фраза
статьи — «Ошиблись не в цифре — в знаке» (её же просил вынести роаст).

**13. Раздел «Реализация».** ✅ Хуки ушли; осталось две темы — харнес и деградация контекста,
причём вторая теперь работает на первую (длинный контекст — ещё одна причина сужать задачу).
Харнесу дан объём: к общему определению добавлен список из четырёх конкретных гейтов (форматтер/
линтер в pre-commit, типы и схемы на границах, структурный тест на запрещённую зависимость,
закрытый merge при красном CI) и вывод, что ни один из них не про ИИ. Пять вопросов к числу из
пресс-релиза вынесены **отдельным разделом** «Пять вопросов к любому числу из пресс-релиза»
между фазами, с явной рамкой «этот гейт стоит поперёк всех шести фаз», нумерованным списком —
и туда же переехали Devin и OpenAI как мотивировка.

**14. «Ревью и безопасность».** ✅ Раздел переименован в «Ревью и доступы: контроль живёт снаружи
агента» — под то, что в нём реально доказывается, — и два аргумента разведены явно: «Ревью тут не
спасает по определению: к моменту, когда открыт PR, данных уже нет. Поэтому **первая линия** —
права, а не чтение диффов» (четыре меры списком) → «**Вторая линия** проседает тише» (ревью).
Посылка «ИИ-код выглядит аккуратнее человеческого» (утверждение без источника) снята.

**15. Метрики ревью.** ✅ Осталось одно чистое сравнение с базой: «По выборке из 33 596 PR,
открытых агентами, 61,38% не получают вообще никакой зафиксированной ревью-активности — ни
комментария, ни запроса изменений, ничего». Пара «25,21% против 8,08%», «+8,0 п.п.» и «+4,4 п.п.»
вырезаны; от второй работы остался только её качественный вывод со ссылкой («выигрыш в скорости
не переходит в качество ревью») и база выборки — чисел на двух шкалах рядом больше нет.

**16. «Требования» и «Архитектура» не брали ship-bar.** ✅ Закрыт показом, без новых источников.
- *Архитектура* открывается фрагментом ADR, собранного агентом по готовому diff (выноска из трёх
  строк, помечена «Фрагмент составлен для примера, но узнаётся в любом репозитории»), и читатель
  сам видит подлог: «Каждая строка — правда, и причины здесь нет ни одной. Postgres выбрали не за
  транзакции: он уже стоял в `docker-compose`». Дальше — почему это нечем пересмотреть через год.
- *Требования* открываются сценой и списком трёх молчаливых допущений, каждое из которых модель
  приняла за автора, плюс отдельный абзац про цену: чинить придётся решением, которого никто не
  принимал, а в базе уже лежат брони по обоим прочтениям правила. Пример помечен как
  иллюстрация словом «Представьте», за измеренный факт не выдан.
- У fitness-функции появился конкретный инвариант («модуль биллинга не импортирует ничего из
  модуля отчётов»), у mutation score — работающий порог (собственный прошлый прогон), вместо
  абстракции и вместо чужих 15%/2,4% в роли планки.

**17. Формат.** ✅ Пять списков: три молчаливых допущения, два вопроса соразмерности, три
артефакта архитектуры, четыре гейта харнеса, четыре меры контроля доступа — плюс нумерованные
пять вопросов к цифре. Выноски: эпиграф, фрагмент ADR, финальный вопрос. 22 жирных выделения
несущего. Все абзацы длиннее 95 слов разбиты (было 8 абзацев 100+, стало 0). Числа везде цифрами
(«16 мейнтейнеров», «10 вернувшихся», «47 новых») — разнобой прописью/цифрами снят.

**18. Оправдания перед воображаемым критиком.** ✅ Все три вырезаны: «но даже с поправкой на
риторику направление верное» → «Соотношение он не измерял; адрес назвал точно»; «Не по всем
пятистам задачам бенчмарка — именно по этому сложному хвосту» → снято (база названа в той же
фразе); «но механика от этого не менее реальна» → снято, осталась только честная половина
(«Это иллюстрация механики из самого отчёта, а не измеренное среднее по индустрии»).

**19. Однотипные входы и выходы разделов.** ✅ «Интуитивно» / «интуиция» — 0 вхождений в тексте,
«Практический вывод» — 0. Входы: реплика заказчика, фрагмент ADR, утверждение про ширину задачи,
рамка сквозного инструмента, кейс Meta, «Девять секунд.», сцена с просевшими метриками. Выходы
тоже разные: «Соразмерность проверяется двумя вопросами», «Чинится это тремя артефактами»,
«Практически харнес — это несколько скучных вещей», «Проверять стоит не объём покрытия…»,
«первая линия — права», «Порядок действий из этого следует обратный привычному».

**20. Финал приземлялся трижды.** ✅ Контур введён раньше — в тезисном абзаце вступления («спека →
ADR (запись об архитектурном решении) → план → PR → журнал инцидента → и снова спека») — и
закрывается один раз. Перечисление шести разделов (~50 слов) вырезано; отдельный абзац-возврат
к METR свёрнут в первую фразу финала. Осталось: один абзац + выноска-вопрос.

**21. Заголовок.** ✅ В текст поставлен вариант A (см. ниже).

**22. Ссылки.** ✅ 6 → 16 инлайн-ссылок; все резолвлены в `fact-check.md` или
`fact-check-round2.md`, новых источников не добавлялось. Единственное утверждение, оставшееся
без ссылки и снятое поэтому, — «Cursor в собственном блоге признаёт, что модели конкурентов
иногда лучше» (не резолвлено ни в одном из двух фактчеков).

---

## Варианты заголовка

- **A. «Среднего ответа про ИИ в разработке больше нет. Есть шесть гейтов»** — поставлен в текст.
  Держит ровно то, что даёт тело: METR-поворот (среднего ответа нет) плюс груз (шесть гейтов).
  Называет количество, поэтому не читается как абстрактная дихотомия.
- B. «Шесть фаз разработки — шесть гейтов для ИИ». Короче и конкретнее, но теряет хук: непонятно,
  почему вообще понадобилось резать на фазы.
- C. «METR больше не может измерить пользу ИИ. Что ставить вместо среднего ответа». Сильнее всего
  в ленте, но ставит на имя, которое узнает не всякий читатель, и обещает разбор METR, а не чек-лист.

Прежний вариант («ИИ в разработке: интуиция vs дисциплина») снят: антитеза ложновата — статья не
доказывает, что интуиция плоха, она доказывает, что самооценка не измеряется. Термин «гейт»
оставлен и поясняется при первом употреблении в тексте: «проверку, которую не пройти уговором,
её либо выполнили, либо дальше не едем». `brief.md` не правился — если владелец утверждает
заголовок, slug и title в брифе синхронизирует оркестратор.

---

## Что НЕ сделано и почему

- **Тезис не в первом абзаце буквально.** Ship-bar формулирует «тезис ясен в первом абзаце»;
  сейчас полный тезис — на 267-м слове (пятый короткий абзац), поворот — на 201-м. Дальше
  поднимать его можно только ценой хука METR, а хук — единственное место, где статья объясняет,
  *почему* среднего ответа нет; ломать его ради формального пункта я не стал. Отмечаю как
  сознательный компромисс, а не как закрытый пункт.
- **Новых кейсов в «Требования» и «Архитектуру» не добавлено** — оба примера иллюстративные и
  помечены как иллюстрации. Задание прямо запрещает добавлять источники сверх резолвленных
  в двух фактчеках, а публичного постмортема «баг оказался непроговорённым допущением» среди них
  нет. Если владелец захочет настоящий кейс — это отдельный заход фактчекера.
- **Cursor как третий пример вендорских цифр снят, а не заменён.** Третьего резолвленного случая
  в фактчеках нет; текст теперь честно говорит «в обоих случаях методику раскрыл сам вендор».
- **Происхождение материала (Лекция 4) в текст не вписано** — mirror относит это к nudge, а не к
  must, а `brief.md` числит протечку кухни в DO NOT. Решение за владельцем.
- **Эмодзи-карточки (💡/📌)** не использованы: калибровка по отгруженной аналитической статье
  `20260901_transformers-vaswani/ru.md`, которая обходится выносками, буллетами и жирным.
  Открытый калибровочный вопрос из `mirror.md` — к владельцу.
- **en.md не трогался** — правился только русский текст, как и было задано.
