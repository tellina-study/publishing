## Fact-check round 3: agent-config-ladder

Раунд 3 закрывает ровно три источника, которые статья называет по имени, но не линкует.
Закрытое в `fact-check.md` и `fact-check-round2.md` не переоткрывалось.

**ШАГ 0 (план раунда):**
1. Cognition — открыть сам пост, вытащить дату, автора, дословные принципы про общий контекст.
   Разрешение = страница открылась + есть цитируемая фраза.
2. dev.to — найти сам пост по точной строке «Rules in prompts are requests»; подтвердить, что
   200 строк, 258 файлов и формула — из него; вытащить автора и дату.
   Разрешение = один пост, в котором есть все три факта.
3. CrewAI — найти конкретные issue с номерами/датами/статусом.
   Разрешение = ≥2–3 независимые жалобы на одно и то же; иначе формулировку ослабляем.

---

### Таблица

| # | Утверждение в статье (строка `ru.md`) | Источник (резолвленная ссылка) | Дословная цитата / свидетельство | Вердикт | Что делать |
|---|---|---|---|---|---|
| 1 | Cognition в манифесте «Don't Build Multi-Agents» (12 июня 2025-го) предупреждает: если шаги зависят друг от друга и им нужен общий контекст, разделение на субагентов теряет именно то, что было важно (стр. 121) | https://cognition.com/blog/dont-build-multi-agents (старый адрес `cognition.ai/blog/dont-build-multi-agents` даёт 301 на `cognition.com`) | Шапка: «Don't Build Multi-Agents · By Walden Yan · 06.12.25». Принцип 1: «Share context, and share full agent traces, not just individual messages». Принцип 2: «Actions carry implicit decisions, and conflicting decisions carry bad results». Про архитектуру «разбей на части → субагенты → склей»: «This is a tempting architecture, especially if you work in a domain of tasks with several parallel components to it. However, it is very fragile.» Итог: «it is evident that in 2025, running multiple agents in collaboration only results in fragile systems. The decision-making ends up being too dispersed and context isn't able to be shared thoroughly enough between the agents.» | **Verified this run** (дата, автор, тезис) | Поставить ссылку на `cognition.com`, не на `.ai` |
| 1a | Дата «12 июня 2025» | там же | В шапке стоит `06.12.25`. Формат — MM.DD.YY: в том же списке статей есть `05.27.26` и `06.04.26`, что однозначно читается только как May 27 / June 4 | **Verified this run** | Дату оставить как есть |
| 1b | Формулировка «субагенту нужен весь ход предыдущих рассуждений, не только их итог» (стр. 121) | там же | Принцип 1 дословно: «share full agent traces, **not just individual messages**»; плюс про Claude Code: «The subtask agent lacks context from the main agent that would otherwise be needed to do anything beyond answering a well-defined question.» | **Verified this run** — это точный пересказ Принципа 1 | Оставить; при желании дать дословную вставку (см. ниже) |
| 2 | «Живой кейс с dev.to (2026): разработчик держал в CLAUDE.md 200 строк правил…» (стр. 35) | https://dev.to/minatoplanb/i-wrote-200-lines-of-rules-for-claude-code-it-ignored-them-all-4639 — «I Wrote 200 Lines of Rules for Claude Code. It Ignored Them All.», автор DavidAI311 (`@minatoplanb`), опубликовано 2026-03-08 (dev.to API: `published_at: 2026-03-08T17:30:37Z`) | «My CLAUDE.md file — the instruction file that tells Claude how to behave — has over 200 lines of rules. Every line has a date. Every line has an incident behind it.»; «200 lines of rules. All written because Claude failed. All loaded every session. All ignored.» | **Verified this run** | Добавить ссылку, автора и дату |
| 2a | «…поверх **базы кода** на 258 файлов» (стр. 35) | там же | В посте 258 — это **база знаний (Obsidian-хранилище)**, а не кодовая база: строка таблицы инцидентов «2026-03-08 · 258 knowledge base files, never retrieved before a task»; таблица «что пробовал»: «Knowledge base · 258 Obsidian vault files · Writes to it, never reads from it» | **Wrong** — подмена сущности | **Обязательно исправить:** не «база кода на 258 файлов», а «хранилище заметок на 258 файлов, к которому агент ни разу не обратился перед задачей» |
| 2b | «Правила остались „write-only“» (стр. 35) | там же | Автор применяет «write-only» к базе знаний, не к правилам: «Knowledge bases become write-only databases». Про правила у него другие слова: «Detailed rules · 200-line CLAUDE.md · **Read but not followed**» и «All loaded every session. All ignored.» | **Inferred from sources** — термин перенесён на соседнюю сущность | Развести: хранилище — «write-only», правила — «прочитаны, но не соблюдены» (это и есть наш тезис ступени, только чужой цитатой, а не чужим ярлыком на чужом месте) |
| 2c | «Rules in prompts are requests. Hooks in code are laws» — вывод автора (стр. 35) | там же | Дословно, отдельной строкой после разбора: «The only safeguard that actually works is Hooks. Why? Because hooks enforce via code, not prompts. Claude doesn't get to choose whether to comply — the hook blocks the action regardless.» → «**Rules in prompts are requests. Hooks in code are laws.**» | **Verified this run** — цитата точная, атрибуция верная | Оставить дословно, со ссылкой |
| 2d | Надёжность источника (мы называем это «живым кейсом») | там же + перепроверка его ссылок | Пост сам признаётся: «This article was written after I told Claude to "confess your failures to the world." Then I edited it.» 4 реакции, 2 комментария. **Но** все пять issue, на которые он ссылается, реальны и названия совпадают: `anthropics/claude-code` #15443 «Claude ignores explicit CLAUDE.md instructions while claiming to understand them», #6120 «…ignores most (if not all) the instructions from CLAUDE.md», #18660 «CLAUDE.md instructions are read but not reliably followed — need enforcement mechanism», #24318 «…ignores explicit user instructions and acts without approval», #668 «Claude not following Claude.md / memory instructions» — все пять резолвятся, все закрыты (проверено через GitHub API) | **Verified this run** (5/5 проверяемых ссылок поста сходятся) | Источник годен. Но он — личный анекдот, частично написанный моделью: держать его как иллюстрацию, а не как доказательство. Усиление бесплатно: #18660 — это буквально наша ступень («read but not reliably followed — need enforcement mechanism»), и это трекер Anthropic, а не блогопост |
| 3 | «В issues самого CrewAI разработчики **раз за разом** сообщают одно и то же… с включённой памятью агент путает контекст между задачами **и тянет уже отменённые решения**» (стр. 47) | Поиск по всем issue `crewAIInc/crewAI` со словом memory в заголовке (125 шт.) + полнотекстовый поиск | Профильный репорт **ровно один**: [#2242 «[BUG] CrewAI is not accurate when retrieving memories.»](https://github.com/crewAIInc/crewAI/issues/2242), создан 2025-02-27, закрыт 2025-05-30 как `not_planned` (по stale-боту). Дословно: «The memories are being saved without any issues. However, it is not accurate when retrieving memories… the agent does not understand that question 5 refers to my last message about abstract classes. Instead, it goes back to discussing the first topic… the documents it retrieves are indeed related to the first topic of the conversation. The scores are also higher for th[em]». В треде: ссылка мейнтейнера на CLI chat, вопрос «Could you guys share a code snippet to be able to reproduce that?» — репро так и не прислали, никто не написал «то же самое у меня». Форумный дубль [community.crewai.com/t/…/4239](https://community.crewai.com/t/crewai-is-not-accurate-when-retrieving-memories/4239) — **тот же автор в тот же день** (CAESAR_TIAGO_FURLANE, 27.02.2025), то есть не вторая жалоба, а та же | **Not verified** в части «раз за разом» и «тянет отменённые решения» | **Ослабить.** «Раз за разом сообщают одно и то же» не держится: это один репорт, закрытый как stale без воспроизведения |
| 3a | Есть ли вообще другие тикеты про качество выдачи памяти | GitHub API, `crewAIInc/crewAI` | Есть, но про другие механизмы и не от пользователей-практиков: [#6747](https://github.com/crewAIInc/crewAI/issues/6747) «Memory recall dedup keeps whichever duplicate a thread finished first, not the best score» (2026-07-31, closed `not_planned`) — «the same query can return the same record at a different rank on consecutive runs»; [#7290](https://github.com/crewAIInc/crewAI/issues/7290) «Memory.forget() races pending background saves…: forgotten content is resurrected after drain_writes()» (2026-09-05, **open**) — «When the background save lands afterwards, the "forgotten" content is resurrected and `recall()` returns it», подтверждено двумя другими участниками треда; [#6043](https://github.com/crewAIInc/crewAI/issues/6043) (open, 2026-06-04) — cross-agent memory poisoning, но это feature request, а не наблюдённое поведение | **Verified this run** (что эти тикеты существуют и говорят именно это) | Если хотим сохранить мысль «память иногда делает хуже» — опереть её на эти два номера явно, с честной пометкой, что #6747/#7290 — баги уровня библиотеки, а не рассказы пользователей |
| 3b | Побочная сверка с раундом 1 (не переоткрытие) | [#5800](https://github.com/crewAIInc/crewAI/issues/5800) «Bench'd Independent Benchmark Results for CrewAI Memory» (2026-05-13, closed `not_planned`) | Дословно: «LongMemEval v1.0 · **46.0%** · 500 · Verified»; «The LLM baseline (no memory) scores **57.6%**». Это промо-issue от коммерческой платформы benchd.ai, закрытое мейнтейнерами как not_planned | — (справка) | Подтверждает решение раунда 1 убрать 46,0/57,6: теперь известно и происхождение цифр — маркетинговый тикет вендора бенчмарка, не публикация CrewAI. **Не возвращать** |

---

### Must fix before ship

- **#2a — фактическая ошибка.** «База кода на 258 файлов» — в источнике это база знаний (Obsidian-хранилище) на 258 файлов, и суть кейса именно в том, что агент в неё не заглядывал. Сейчас мы приписываем автору утверждение, которого он не делал, и заодно ломаем логику его же примера.
- **#3 — «раз за разом» не подтверждено.** Один репорт (#2242), закрытый как not_planned по stale, без воспроизведения, плюс его же копия на форуме. Формулировку «разработчики раз за разом сообщают одно и то же» надо снять. «Тянет уже отменённые решения» в #2242 не описано вовсе (там — прилипание к первой теме диалога).

### Flags

- **#2b** — «write-only» у автора относится к базе знаний, а не к правилам. Перенос ярлыка меняет чужую формулировку; чинится одной фразой.
- **#2d** — dev.to-пост частично написан моделью (признание в последней строке), 4 реакции. Его проверяемые ссылки сошлись 5/5, так что как иллюстрация он годен, но нести на себе всю ступень не должен. `anthropics/claude-code#18660` («read but not reliably followed — need enforcement mechanism») — более прочная опора под тот же тезис, и она уже из трекера Anthropic.
- **#1** — ссылку ставить на `cognition.com`, а не `cognition.ai` (там 301).
- **#3a** — если берём #7290, честно пометить: это баг гонки в самой библиотеке (забытое возвращается в `recall()`), а не отчёт практика о том, что «агент спутал задачи». Иначе повторим ровно тот приём, за который сами ругаем чужие статьи.

### Готовые формулировки (черновик для автора, не правка)

- **Пункт 1, дословная вставка:** Cognition формулирует это как принцип: «Share context, and share full agent traces, not just individual messages» — делить надо не задачу, а контекст; и дальше прямо: архитектура «разбить → раздать субагентам → склеить» «is very fragile».
- **Пункт 2:** «Кейс с dev.to (8 марта 2026): разработчик держал в CLAUDE.md больше 200 строк правил — каждая с датой и инцидентом за спиной — и рядом хранилище заметок на 258 файлов, к которому агент ни разу не обращался перед задачей. Его собственный итог по правилам: "Read but not followed", по хранилищу: "Knowledge bases become write-only databases". И вывод, который держит всю эту ступень: "Rules in prompts are requests. Hooks in code are laws."»
- **Пункт 3:** «Память иногда делает агента хуже — и это видно прямо в трекере CrewAI. Репорт [#2242](https://github.com/crewAIInc/crewAI/issues/2242) (февраль 2025, закрыт как not_planned, репро так и не прислали): память сохраняется без ошибок, но на смене темы агент выдаёт ответ по первой теме разговора — у неё выше score. Строгого замера за этим нет, и это единичный репорт, а не вал жалоб; рядом стоят баги уровня библиотеки — [#6747](https://github.com/crewAIInc/crewAI/issues/6747) (ранг выдачи пляшет от запуска к запуску) и открытый [#7290](https://github.com/crewAIInc/crewAI/issues/7290) (забытое всплывает обратно в `recall()`).»

**VERDICT: 2 claims must be fixed** (#2a — неверная сущность «база кода»; #3 — «раз за разом» + «отменённые решения» не подтверждены). Остальное — Verified this run, ссылки резолвятся.
